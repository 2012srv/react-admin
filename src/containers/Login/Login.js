import { useEffect } from 'react';
import Button from 'react-bootstrap/Button';

const Login = (props) => {

    useEffect(() => {

    }, []);

    return (
        <div className="App">
            dfdsf
            <Button>Login</Button>
        </div>
    )
}

export default Login;