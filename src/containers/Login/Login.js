import Button from 'react-bootstrap/Button';

const Login = (props) => {
    return (
        <div className="App">
            dfdsf
            <Button>Login</Button>
        </div>
    )
}

export default Login;