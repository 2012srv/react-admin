import React, { useEffect } from 'react';
import { connect } from 'react-redux';

const Logout = props => {
    useEffect(() => {
        // props.onLogout();
    }, []);

    return '';
};

export default Logout;
